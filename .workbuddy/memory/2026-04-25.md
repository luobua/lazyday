# 2026-04-25 工作日志

## 完成事项

### 规划 lazyday 开放平台文档体系
基于现有 Backend（Spring Boot 4 + WebFlux + DDD）架构，确定如下整体技术方案：
- **Frontend**：Next.js 15 + Ant Design，Monorepo 包含 Developer Portal + Admin Console
- **Edge**：自研 Spring Cloud Gateway，承担鉴权/限流/日志
- **功能范围**：标准版（租户 + AppKey + 限流配额 + 调用日志 + Webhook）

### 生成文档（归档至 docs/）
1. `docs/architecture-overview.md` — 整体系统架构（含架构图、API 路由规划、部署架构）
2. `docs/backend-architecture.md` — 后端技术架构（DDD 分层、待扩展业务域、数据库规范）
3. `docs/edge-architecture.md` — Edge 网关架构（鉴权流程、限流设计、熔断、调用日志）
4. `docs/frontend-architecture.md` — 前端架构（Next.js Monorepo 结构、路由设计、状态管理）
5. `docs/requirements-design.md` — 需求设计（功能列表、数据模型 SQL、API 接口清单、里程碑）

### 文档审阅与修订（第一轮）
对 5 份文档进行全面审阅，修正以下关键问题：
- **architecture-overview**：Frontend 标注 SSR、Edge-Backend 标注 WebSocket 双向通信、部署架构补充 CDN
- **backend-architecture**：明确 ID 生成策略（业务表自增序列/日志表雪花算法）、租户与用户关系（注册同步创建 t_user）、AppKey 加密密钥来源、补充领域事件使用场景
- **edge-architecture**：AuthFilter 明确异步 WebClient 调用、限流响应头标注单位、熔断配置改为时间窗口（60s/20次）、调用日志改为 Redis 队列方案、Portal/Admin 路由明确 JWT 过滤器
- **frontend-architecture**：明确 Cookie 方案（Axios 无需手动设置 Authorization）、Middleware 保护所有路由、API 文档使用 swagger-ui-react、补充 Node 环境准备、api-client 支持多 baseURL
- **requirements-design**：Phase 1 跳过邮箱验证（Phase 2 引入）、套餐表补充软删除、补充删除套餐 API、调用日志查询补充分区说明、Webhook 重试明确「永久失败」、新增 Open API 业务接口预留章节、新增邮件服务规划章节

### 新增 AI 大脑功能（RAG + Agent + Workflow）
基于用户新增需求，5 份文档全部更新至 v0.3：
- **architecture-overview**：Backend 新增 RAG/Agent/Workflow/配置下发 4 个业务域；Edge 新增「大脑组装」模块；Frontend 新增 RAG/Agent/Workflow 管理功能；技术选型补充 TTS/ASR
- **backend-architecture**：新增 5.6-5.9 节（RAG 配置域、Agent 配置域、Workflow 配置域、配置下发域），含完整字段定义和下发流程；配置项补充 AI/TTS/ASR 参数
- **edge-architecture**：新增第 10 节「大脑组装」，含配置同步机制（L1/L2 缓存）、BrainAssemblyService 伪代码、请求处理模式表、TTS/ASR 动态切换
- **frontend-architecture**：新增 Portal 路由（/rag /agent /workflow）、第 9 节 RAG/Agent/Workflow 管理界面详细设计（含 ReactFlow 画布）、技术选型补充 ReactFlow
- **requirements-design**：功能模块列表补充 RAG/Agent/Workflow/配置下发；新增 6 张数据表 SQL（t_rag_config / t_rag_document / t_agent_config / t_workflow_config / t_workflow_node / t_tenant_brain_config）；API 接口清单补充 20+ 个接口；里程碑新增 Phase 4（AI 大脑）；第 9 节重写为完整 Open API 接口定义（chat / tts / asr / workflow / agent）

### 核心概念澄清：中枢 vs 大脑（v0.4）
用户明确架构概念：**Frontend + Backend = 中枢（管理/监控/调度）**，**Edge = 大脑（AI 执行单元）**。
关键更新：
- `architecture-overview.md`：架构图重绘，中枢/大脑分层标注；§3 模块说明重新划分（Frontend=中枢界面，Backend=中枢服务，Edge=大脑执行）
- `backend-architecture.md`：新增 §0「中枢定位」；新增 §9 WebSocket 配置章节；配置下发域补充 WebSocket 连接初始化说明
- `edge-architecture.md`：新增 §0「大脑定位」；§10 大脑组装重写，新增 Tool 注册表说明、缓存降级行为（stale-while-revalidate）、NPE 防御、异步日志推送；新增 §11 WebSocket 客户端；新增 BRAIN_NOT_READY 错误码
- `frontend-architecture.md`：新增 §0「中枢定位」；修正 §9 重复编号；补充检索测试接口返回格式；补充条件表达式 DSL 说明；补充下发失败排查机制
- `requirements-design.md`：新增 §0「中枢与大脑概念」；补充 t_tenant_brain_config 审计字段；补充 /rag/{id}/search 返回格式；明确 /brain-configs/{tenantId}/dispatch 为异步下发；补充 AI 对话非流式响应示例

### 文档二次审阅与修订（v0.4 终版）

**高优先级修复**：
- **R-1**：`requirements-design.md` §6.3 `POST /rag/{id}/search` 返回格式补全标准外层包装 `{code, message, data.items}`，与通用响应规范一致
- **R-2**：`requirements-design.md` §5.11 `t_tenant_brain_config` 补充 `create_user`、`update_user`、`deleted` 审计字段，与 `BaseAllUserTime` 规范对齐

**中优先级修复**：
- **M-1**：`architecture-overview.md` 架构图说明补充 Frontend 定位——归属于中枢体系的可视化管控界面，独立部署但归属中枢体系
- **M-2**：`frontend-architecture.md` §4 路由说明中「中枢内部路由」改为准确的「Backend 面向 Portal/Admin 的 API」；同步修正 `architecture-overview.md` §4 API 路由注释
- **M-3**：`requirements-design.md` §5.11 `dispatch_status` 注释补充分值说明 `pending/dispatched/failed`

**低优先级修复**：
- **L-1**：`backend-architecture.md` §5.9 `ConfigDispatchAggregation` 标注建议名称 `BrainConfigAggregation`（可选采纳）
- **L-3**：`edge-architecture.md` §10.2 缓存降级行为补充：所有 Edge 实例均断开时返回 `BRAIN_NOT_READY`（HTTP 503），不降级处理

**修订记录**：所有 5 份文档头部新增修订记录表（v0.1 → v0.4 完整变更履历）